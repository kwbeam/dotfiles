# ----------
# Python
# ----------
export PATH="/home/kbeam/miniconda2/bin:$PATH"

# ----------
# Node Version Manager - nvm
# ----------
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
[[ -r $NVM_DIR/bash_completion ]] && . $NVM_DIR/bash_completion

# ----------
# Local Unix tools
# ----------
PATH=$PATH:~/local/bin

export PATH
